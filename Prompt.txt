I am building a hackathon MVP in the next 4 hours. We are solving "Which Tree Falls First" for Halifax Urban Forestry. 

The problem: 290 tree requests are waiting. Every request needs a site visit. We need to triage them based on text descriptions to find the dangerous ones first.

Please build a Next.js (App Router) + Tailwind CSS web application. 

Here are the exact requirements:

1. MOCK DATA: Generate a realistic JSON array of 20 mock 311 tree requests in Halifax. Include: id, date, location (use real Halifax streets like Agricola St, Robie St, Quinpool Rd), description (make some sound highly dangerous like "leaning heavily over house after storm", some moderate like "branch touching power line", and some cosmetic like "needs trimming for aesthetics"), and a placeholder for photo_url.

2. THE TRIAGE ENGINE (The Core Feature): 
Create a function `triageRequest(description)` that uses the Anthropic SDK (claude-3-haiku-20240307 for speed/cost) to analyze the text. 
The LLM prompt should ask Claude to return a strict JSON object: 
{ "risk_score": 1-10, "priority": "HIGH" | "MEDIUM" | "LOW", "reasoning": "short string", "is_unsure": boolean }
- HIGH: Imminent hazard, leaning on structure, power lines.
- MEDIUM: Dead branches, minor power line issues.
- LOW: Cosmetic, routine pruning.
- is_unsure: true if the description is vague (e.g., "tree looks weird") and needs human review.

3. THE UI (Dashboard):
- Top section: Summary cards (Total Requests, High Priority, Needs Human Review).
- Main section: A sorted list/table of the requests, sorted by risk_score descending. 
- Each row shows: Location, Description, Risk Score (color coded: Red for High, Yellow for Med, Green for Low), Priority, and a "Needs Review" badge if is_unsure is true.
- Clicking a row expands it to show the full details and a placeholder image box.

4. TECH STACK & RULES:
- Use Next.js 14 (App Router), Tailwind CSS, and shadcn/ui if possible for clean components.
- Use the `@anthropic-ai/sdk` for the LLM calls. (I will provide the API key in .env.local).
- Keep it in as few files as possible for speed. Put the mock data in a `data/mockTrees.ts` file.
- Make the UI look like a professional internal government tool (clean, high contrast, data-dense).

Please write the complete code for all necessary files. Start with the project structure, then the mock data, then the triage API route, and finally the frontend components.
